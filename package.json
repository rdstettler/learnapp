{
  "name": "antigravity",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd frontend && npm run start",
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build",
    "migrate-trigger": "node scripts/trigger-migrate.js",
    "publish": "vercel deploy --prod && npm run migrate-trigger"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "dotenv": "^17.2.3",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
